## Node 1 — what does the method give in the CLI?  [GOLDEN ✓]
**Crystal:** The chat is disposable scratch; drafts/spine/<topic>.spine.md is the message you keep editing. The crystal goes to the file, the noise stays in the chat.
**How it works:** a slash command cannot invoke /clear·/rewind (client built-ins) — so durable memory moves to a file and the human does context surgery (cite: hooks.md §Limitations).
**Epitaphs:** "smart compactor as a skill" — rejected: no message-ID, can't trigger compact programmatically.
